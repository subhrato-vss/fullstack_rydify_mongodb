<!doctype html>
<html class="no-js" lang="zxx">

<head>
    <%-include('../partials/head_includes')%>

    <style>

        .error
        {
            color:red;
        }
        .dashboard {
            text-align: center;
            background: linear-gradient(135deg, #ffffff, #f3f4f6);
            box-shadow: 0 8px 16px rgba(0, 0, 0, 0.2);
            padding: 50px 60px;
            border-radius: 30px;
            transition: transform 0.3s ease;
        }

        .dashboard:hover {
            transform: translateY(-10px);
        }

        .stat {
            font-size: 3rem;
            font-weight: bold;
            color: rgb(255, 192, 0);
            text-shadow: 2px 2px 10px rgba(255, 0, 0, 0.3);
        }

        h1 {
            color: #333;
            font-size: 2.5rem;
        }

        p {
            font-size: 1.2rem;
            color: #555;
        }

        .icon {
            font-size: 4rem;
            color: red;
            margin-bottom: 20px;
        }
    </style>
</head>
<body onload="fetchCars()">
<%-include('../partials/user_header')%>

<div class="page-inner"  >
    <h2 class="text-center" style="color: rgb(255, 192, 0);">Welcome <%=name%></h2>
    <div class="dashboard col-md-4 offset-md-4 mt-3 mb-3" >
        <div class="icon">🚗</div>
        <h1>My Bookings</h1>
        <p>Total Cars Bought : <span class="stat" id="carCount">50</span></p>
    </div>
</div>

<%-include('../partials/footer')%>
<%-include('../partials/body_includes')%>
<script>
    async function fetchCars()
    {
        var res=await fetch('/user/fetchMycars');
        res=await res.json();
        var records=res.records;
        document.getElementById("carCount").innerText=records.length;

    }
</script>
</body>
</html>